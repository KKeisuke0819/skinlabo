<div class="row">
	<div class="col-lg-6">
		<h2>ジャンル編集画面</h2>

		<%= form_for [:admin, @genre] do |f| %>
		<table class="table table-hover table-inverse">
			<tr><th>カテゴリー</th>
				<td><%= f.text_field :variety %></td></tr>
		</table>
		<p><%= f.submit "変更を保存する" %></p>
		<% end %>
	</div>
</div>
