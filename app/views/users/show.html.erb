<div class="row">
	<h2>マイページ</h2>
	<h3>登録情報</h3>
   		<div class = "col-lg-8">
    		<table class="table table-inverse">
      			<tr><th>氏名</th>
        			<td><%= current_user.user_name %></td></tr>
      			<tr><th>メールアドレス</th>
        		<td><%= current_user.email %></td></tr>
    		</table>
        <p><%= link_to "編集", edit_user_path(@user.id), class: "btn btn-primary" %></p>
        <p><%= link_to "パスワードを変更する", edit_user_registration_path(@user.id), class: "btn btn-info" %></p>
 		  </div>
</div>
